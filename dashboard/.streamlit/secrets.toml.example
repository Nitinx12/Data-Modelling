# Copy to secrets.toml (gitignored) for local `streamlit run` or paste into
# Streamlit Cloud App settings → Secrets. Never commit real credentials.
[postgres]
host = "your-neon-host.neon.tech"
port = 5432
database = "data_warehouse"
user = "dashboard_reader"
password = "..."
# Optional: sslmode (neon/supabase require sslmode=require)
# sslmode = "require"
